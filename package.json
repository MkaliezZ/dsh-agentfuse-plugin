{
  "name": "@dhms-agentfuse/monorepo",
  "version": "0.1.1",
  "private": true,
  "type": "module",
  "description": "AgentFuse: deterministic fail-closed tool-call authorization with evidence — framework-agnostic core + DeepSeek Harness adapter",
  "license": "Apache-2.0",
  "scripts": {
    "build:core": "tsc -b packages/core"
  }
}
